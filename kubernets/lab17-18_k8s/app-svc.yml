apiVersion: v1
kind: Service
metadata: 
  name: app-service
  namespace: ivolve
spec:
  type: ClusterIP
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
